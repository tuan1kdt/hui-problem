\subsection{Experimental Setup}

All three algorithms (TKU, TKO, THUI) are implemented in Go. Experiments were conducted on a machine with Apple M-series CPU, 16GB RAM, running macOS. We used the standard SPMF input format: \texttt{items:TU:utilities}.